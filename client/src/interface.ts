
export interface menuType { 
    id: number,
    image: string,
    title: string,
    featured: boolean
}

export interface locationType {
    id: string,
    title:string,
    address:string,
    hours:string
}